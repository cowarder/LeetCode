class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode *p = NULL , *q = NULL;
        while(head){
            q = head->next;
            head->next = p;
            p = head;
            head = q;
        }
        return temp;
    }
};